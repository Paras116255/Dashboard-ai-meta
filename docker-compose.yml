services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://adpilot:adpilot_secret@postgres:5432/adpilot_db?schema=public
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=adpilot_super_secret_jwt_key_32_bytes_long_min!
      - ENCRYPTION_KEY=adpilot_meta_token_encryption_key_32bytes
      - USE_MOCK_META_API=true
      - AI_PROVIDER=mock
      - APP_VERSION=v1.0.0
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["npm", "run", "worker"]
    environment:
      - DATABASE_URL=postgresql://adpilot:adpilot_secret@postgres:5432/adpilot_db?schema=public
      - REDIS_URL=redis://redis:6379
      - USE_MOCK_META_API=true
      - AI_PROVIDER=mock
      - APP_VERSION=v1.0.0
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  postgres:
    image: postgres:15-alpine
    container_name: adpilot_postgres
    restart: always
    environment:
      POSTGRES_USER: adpilot
      POSTGRES_PASSWORD: adpilot_secret
      POSTGRES_DB: adpilot_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U adpilot -d adpilot_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: adpilot_redis
    restart: always
    ports:
      - "6379:6379"

volumes:
  postgres_data:
